# Speedi — UNC's #1 Delivery Service

Static marketing site for Speedi delivery (Chapel Hill, NC). Plain HTML/CSS/JS — no build step.

## Local dev

```
python3 -m http.server 8741
```

## Deploy

Deploys as-is on Vercel (framework preset: Other, no build command, output dir: root).
